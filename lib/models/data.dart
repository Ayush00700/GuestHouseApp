// import 'package:flutter/material.dart';
import 'package:guesthouseapp/models/users.dart';

class Data {
  List<User> users;

  // void getUsers() {
  //   StreamBuilder<QuerySnapshot>(
  //     stream: _firestore.collection('admins').snapshots(),
  //     builder: (context, snapshot) {
  //       final adminsAdded = snapshot.data.documents;
  //       admins = [];
  //       for (var adminIndi in adminsAdded) {
  //         admins.add(Admin(name: adminIndi.data['name']));
  //       }
  //       return AdminBuilder();
  //     },
  // );
  // notifyListeners();
  // }
}
