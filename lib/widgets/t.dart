import 'package:flutter/material.dart';

class AAS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),

      // body: ,
    );
  }
}

class name extends StatelessWidget {
  final Widget asd;

  const name({Key key, this.asd}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
        // child: child,
        );
  }
}
